{% extends 'layout.html.twig' %}
{% block title %}{{ page_title }}{% endblock %}
{% block content %}
    <header>
        <h1><a href="{{ landing_page }}">CustomPcSpecialists</a></h1>
    </header>
    <div class="content-div" id="login_form">
        <h2  id="login_heading">{{ heading }}</h2>
        <form action="{{ action }}" method="post">
            <label for="login_username">Username* </label>
            <input type="text" name="username" id="login_username" required />
            <label for="login_password">Password* </label>
            <input type="password" name="password" id="login_password" required />
            <input type="submit" name="login" value="Login" id="login_button" />
        </form>
    </div>
{% endblock %}