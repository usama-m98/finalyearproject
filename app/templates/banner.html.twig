{% extends 'layout.html.twig' %}
{% block title %}{{ page_title }}{% endblock %}
{% block banner %}
    <div id="banner-div">
    <header>
        <h1><a href="{{ landing_page }}">CustomPcSpecialists</a></h1>
        {% if signIn.active_admin %}
            <div class="menu" id="menu-toggle">{{ signIn.user }}</div>
            <nav id="menu-nav">
                <a href="admininterface">Admin Interface</a>
                <a href="logout">Logout</a>
            </nav>
        {% elseif signIn.active_user %}
        <div class="menu" id="menu-toggle">{{ signIn.user }}</div>
        <nav id="menu-nav">
            <a href="youraccount">Your Account</a>
            <a href="#">Your Orders</a>
            <a href="logout">Logout</a>
        </nav>
            {% else %}
                <div id="login-bar">
                    <a href="{{ login }}">Login</a>
                    <a href="{{ signup }}">Sign Up</a>
                </div>
        {% endif %}
        <nav>
            <a href="desktops">Desktop PC</a>
            <a href="#">Laptops</a>
            <a href="contact">Contact Us</a>
            <a href="#">Monitors</a>
            <a href="#">Peripherals</a>
        </nav>

        <form action="{{ action }}" method="post" id="search">
            <input type="text" maxlength="100" size="100">
            <input type="submit" value="search">
        </form>
    </header>
    <div id="clear-div"></div>
{% endblock %}